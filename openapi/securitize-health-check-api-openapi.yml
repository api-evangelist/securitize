openapi: 3.2.0
info:
  version: 3.0.0
  title: Securitize Domains Health Check API
security:
- ApiKeyAuth: []
tags:
- name: Health Check
paths:
  /v1/health:
    get:
      operationId: AppController_root
      summary: ''
      description: For Health check
      parameters: []
      responses:
        '200':
          description: ''
      tags:
      - Health Check
components:
  securitySchemes:
    ApiKeyAuth:
      type: apiKey
      in: header
      name: Authorization
      description: API Key Format as "apiKey &lt;key_id&gt;:&lt;key_secret&gt;"
